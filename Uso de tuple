#include <iostream>
#include <tuple>
using namespace std;

int main() {
    tuple<int, string, double> t = make_tuple(1, "Ejemplo", 3.14);
    cout << "Entero: " << get<0>(t) << ", String: " << get<1>(t)
         << ", Double: " << get<2>(t) << endl;
    return 0;
}
