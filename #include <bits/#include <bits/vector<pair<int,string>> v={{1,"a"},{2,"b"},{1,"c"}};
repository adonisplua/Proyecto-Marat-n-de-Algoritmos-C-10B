#include <bits/stdc++.h>
using namespace std;
int main(){
    vector<pair<int,string>> v={{1,"a"},{2,"b"},{1,"c"}};
    sort(v.begin(),v.end());
    for(auto &p:v) cout<<p.first<<":"<<p.second<<" ";
    cout<<"\n";
}
