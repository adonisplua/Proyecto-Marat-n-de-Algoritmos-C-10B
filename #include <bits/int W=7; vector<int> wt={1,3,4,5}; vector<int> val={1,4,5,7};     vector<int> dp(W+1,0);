#include <bits/stdc++.h>
using namespace std;
int main(){
    int W=7; vector<int> wt={1,3,4,5}; vector<int> val={1,4,5,7};
    vector<int> dp(W+1,0);
    for(int i=0;i<(int)wt.size();i++)
        for(int w=W;w>=wt[i];w--)
            dp[w]=max(dp[w],dp[w-wt[i]]+val[i]);
    cout<<dp[W]<<"\n";
}
