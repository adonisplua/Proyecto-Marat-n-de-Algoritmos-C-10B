#include <iostream>
using namespace std;

int contarPares(int n) {
    if (n == 0) return 0;
    int digit = n % 10;
    return (digit % 2 == 0 ? 1 : 0) + contarPares(n / 10);
}

int main() {
    int num = 123456;
    cout << "Dígitos pares: " << contarPares(num) << endl;
    return 0;
}
