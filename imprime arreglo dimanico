#include <iostream>
using namespace std;

void imprimir(int* arr, int n) {
    for (int i = 0; i < n; i++)
        cout << arr[i] << " ";
    cout << endl;
}

int main() {
    int n = 4;
    int* arr = new int[n]{1, 2, 3, 4};
    imprimir(arr, n);
    delete[] arr;
    return 0;
}
