Eliminar primera ocurrencia de valor (modifica array)
int eliminarPrimera(int arr[], int n, int val) {
    int i;
    for (i = 0; i < n; i++) {
        if (arr[i] == val)
            break;
    }
    if (i == n) return n; // no encontrado
    for (int j = i; j < n-1; j++)
        arr[j] = arr[j+1];
    return n - 1;
}
