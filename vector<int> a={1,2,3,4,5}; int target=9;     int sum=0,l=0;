#include <bits/stdc++.h>
using namespace std;
int main(){
    vector<int> a={1,2,3,4,5}; int target=9;
    int sum=0,l=0;
    for(int r=0;r<a.size();r++){
        sum+=a[r];
        while(sum>target){ sum-=a[l]; l++; }
        if(sum==target) cout<<"Subarray: "<<l<<"-"<<r<<"\n";
    }
}
