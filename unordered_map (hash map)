#include <iostream>
#include <unordered_map>
using namespace std;

int main() {
    unordered_map<string,int> um;
    um["x"]=1;
    um["y"]=2;
    um["z"]=3;

    for(auto &[key,value] : um)
        cout << key << ": " << value << endl;
    return 0;
}
