#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> v = {1,2,2,3,2,4};
    int veces = count(v.begin(), v.end(), 2);
    cout << "El número 2 aparece " << veces << " veces" << endl;
    return 0;
}
