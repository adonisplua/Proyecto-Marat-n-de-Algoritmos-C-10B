#include <iostream>
using namespace std;

int contarDigitos(int n) {
    if (n == 0) return 0;
    return 1 + contarDigitos(n / 10);
}

int main() {
    int num = 12345;
    cout << "Número de dígitos: " << contarDigitos(num) << endl;
    return 0;
}
