#include <iostream>
#include <set>
using namespace std;

int main() {
    set<int> s = {5, 2, 8, 5};
    s.insert(10);
    s.erase(2);
    for(int x : s) cout << x << " ";
    cout << endl;
    cout << "¿Existe 5? " << (s.count(5) ? "Sí" : "No") << endl;
    return 0;
}
