Función que recibe un array y un número, y rota el array n posiciones hacia la derecha
void rotarDerecha(vector<int>& arr, int n) {
    int size = arr.size();
    n = n % size;
    reverse(arr.begin(), arr.end());
    reverse(arr.begin(), arr.begin() + n);
    reverse(arr.begin() + n, arr.end());
}
