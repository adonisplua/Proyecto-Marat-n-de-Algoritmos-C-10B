Función que recibe un array y retorna la suma de elementos en posiciones múltiplos de 3
int sumaPosMult3(const vector<int>& arr) {
    int suma = 0;
    for (size_t i = 0; i < arr.size(); i++)
        if (i % 3 == 0)
            suma += arr[i];
    return suma;
}
