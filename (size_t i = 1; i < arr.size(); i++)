. Función que recibe un array y retorna true si está ordenado ascendentemente
bool estaOrdenado(const vector<int>& arr) {
    for (size_t i = 1; i < arr.size(); i++)
        if (arr[i] < arr[i-1])
            return false;
    return true;
}
