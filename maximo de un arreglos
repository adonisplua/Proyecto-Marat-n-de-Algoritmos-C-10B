#include <iostream>
using namespace std;

int main(){
    int arr[]={5,8,2,9,3};
    int maxVal=arr[0];
    for(int i=1;i<5;i++)
        if(arr[i]>maxVal) maxVal=arr[i];
    cout<<"Máximo: "<<maxVal<<endl;
    return 0;
}
