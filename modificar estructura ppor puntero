#include <iostream>
using namespace std;

struct Persona {
    string nombre;
    int edad;
};

void modificar(Persona* p) {
    p->edad += 5;
}

int main() {
    Persona persona = {"Ana", 22};
    modificar(&persona);
    cout << "Edad modificada: " << persona.edad << endl;
    return 0;
}
