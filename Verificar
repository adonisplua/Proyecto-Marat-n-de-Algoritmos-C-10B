Verificar si dos arrays son iguales
bool arraysIguales(int arr1[], int n1, int arr2[], int n2) {
    if (n1 != n2)
        return false;
    for (int i = 0; i < n1; i++)
        if (arr1[i] != arr2[i])
            return false;
    return true;
}
