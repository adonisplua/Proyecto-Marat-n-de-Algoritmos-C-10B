#include <iostream>
#include <set>
using namespace std;

int main() {
    set<int> s = {10,20,30,40};
    for(auto it = s.rbegin(); it != s.rend(); ++it) // iterador inverso
        cout << *it << " ";
    cout << endl;
    return 0;
}
